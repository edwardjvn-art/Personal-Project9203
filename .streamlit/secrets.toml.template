# Rename this file to secrets.toml and fill in your details
# Place it at .streamlit/secrets.toml  (never commit this to git)

[credentials]
username = "james"
# Generate a password hash by running:  python hash_password.py
password_hash = "REPLACE_WITH_HASH"

[stocks]
# Free API key from https://finnhub.io  (free tier is plenty)
finnhub_api_key = "YOUR_FINNHUB_API_KEY"

# Stocks to track — use ticker symbols
watchlist = ["AAPL", "TSLA", "VWRL.L", "ISF.L"]
